  <div class="page-header">

    <%= link_to new_veiculo_path, :class => 'btn btn-default' do %>
      <span class="glyphicon glyphicon-plus"></span>
      Cadastrar veículo
    <% end %>

    <h1>Cadastro de veículos</h1>
  </div>

  <div class="table-responsive">
    <table class="table table-striped table-bordered table-hover">
      <thead>
        <tr>
          <th>Marca</th>
          <th>Modelo</th>
          <th>Placa</th>
          <th>Tipo veiculo</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @veiculos.each do |veiculo| %>
          <tr>
            <td><%= veiculo.marca %></td>
            <td><%= veiculo.modelo %></td>
            <td><%= veiculo.placa %></td>
            <td><%= veiculo.tipo_veiculo %></td>
            <td><%= link_to 'Show', veiculo %></td>
            <td><%= link_to 'Edit', edit_veiculo_path(veiculo) %></td>
            <td><%= link_to 'Destroy', veiculo, method: :delete, data: { confirm: 'Tem certeza que deseja remover este veículo?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

<br>


